# Integration Tests

Cross-library and live system-integration tests live here.

Integration tests should verify behavior across subsystem boundaries, such as
project loading, import/export, plugin loading, and workflow invalidation. They
also contain live behavior that depends on OS or backend state, such as
window-system and clipboard behavior.
