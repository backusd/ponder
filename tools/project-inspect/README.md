# Project Inspect Tool

This directory contains the `ponder-project-inspect` tool scaffold.

The current target is a console stub. Real project loading should go through the
same reusable project APIs used by the desktop application once those APIs exist.