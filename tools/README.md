# Tools

Developer and inspection tools live here.

Tools should exercise reusable libraries and avoid becoming hidden application
logic.

## Targets

- `tools/project-inspect/` builds the `ponder_project_inspect` CMake target with
  output name `ponder-project-inspect`.