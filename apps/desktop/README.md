# Desktop Application

This directory contains the `ponder-desktop` application scaffold.

The current target is a console stub that proves executable wiring. Reusable
behavior should continue to live under `libs/` as platform, windowing,
rendering, and UI systems come online.